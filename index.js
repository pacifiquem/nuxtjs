const colors  = require('colors');

const referencer = () => {
    console.log('To install nuxt frontend framework run: '.bold.magenta, "npm init nuxt-app <project-name>".green);
};

module.export = referencer;